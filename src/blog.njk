---
layout: base.njk
title: Blog
description: Updates, flash sales, and new work from Get Inked By J.
permalink: /blog/
---

<header class="page-header">
    <div class="container">
        <span class="section-tag">Updates</span>
        <h1>Blog</h1>
        <p>News, flash sales, and behind-the-scenes content.</p>
    </div>
</header>

<section class="blog-content">
    <div class="container">
        <div class="blog-grid">
            {% for post in collections.posts %}
            <article class="blog-card">
                <div class="blog-card-image">
                    {% if post.data.thumbnail %}
                    <img src="{{ post.data.thumbnail }}" alt="{{ post.data.title }}">
                    {% else %}
                    <img src="https://images.unsplash.com/photo-1611501275019-9b5cda994e8d?w=500&h=300&fit=crop" alt="{{ post.data.title }}">
                    {% endif %}
                </div>
                <div class="blog-card-content">
                    <span class="blog-card-date">{{ post.data.date | dateFormat }}</span>
                    <h3>{{ post.data.title }}</h3>
                    <p>{{ post.data.summary }}</p>
                    <a href="{{ post.url }}" class="blog-card-link">Read More →</a>
                </div>
            </article>
            {% else %}
            <div class="no-posts">
                <p>No blog posts yet. Check back soon!</p>
            </div>
            {% endfor %}
        </div>
    </div>
</section>

<section class="cta">
    <div class="container">
        <h2 class="section-title">Stay Updated</h2>
        <p>Check back here for the latest work and flash announcements.</p>
        <a href="/book/" class="btn btn-primary">Book a Session</a>
    </div>
</section>
