---
layout: base.njk
title: Pay Your Deposit
description: Pay your tattoo deposit to book your session
permalink: /pay-deposit/
---

<section class="pay-deposit-page">
    <div class="container">
        <div class="pay-deposit-box">
            <span class="section-tag">Secure Your Session</span>
            <h1>Pay Your Deposit</h1>
            <p>Select your session length and pay your $200 deposit to book your tattoo appointment.</p>
            
            <div class="session-selector">
                <h3>Select Your Session Length</h3>
                <p class="session-note">Not sure? Check what J quoted you during your consultation.</p>
                
                <div class="session-options">
                    <button class="session-option" data-session="small">
                        <span class="session-name">Small Session</span>
                        <span class="session-duration">2-3 hours</span>
                    </button>
                    <button class="session-option" data-session="medium">
                        <span class="session-name">Medium Session</span>
                        <span class="session-duration">4-6 hours</span>
                    </button>
                    <button class="session-option" data-session="fullday">
                        <span class="session-name">Full Day Session</span>
                        <span class="session-duration">6-7 hours</span>
                    </button>
                </div>
                
                <div class="deposit-amount">
                    <span>Deposit Amount:</span>
                    <span class="amount">$200</span>
                </div>
                
                <button id="pay-button" class="btn btn-primary" disabled style="width: 100%;">Select a Session to Continue</button>
                
                <p class="deposit-note">This deposit goes toward your total tattoo cost. After payment, you'll pick your appointment time.</p>
            </div>

            <div class="deposit-info">
                <h3>Deposit Policy</h3>
                <ul>
                    <li>Deposit is applied to your final tattoo cost</li>
                    <li>48+ hours notice for cancellation = full refund</li>
                    <li>Less than 48 hours notice = deposit forfeited</li>
                    <li>No-shows = deposit forfeited</li>
                </ul>
            </div>
        </div>
    </div>
</section>

<style>
.pay-deposit-page {
    min-height: 80vh;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--dark);
    padding: 10rem 0 4rem;
}

.pay-deposit-box {
    background: var(--gray);
    padding: 3rem;
    max-width: 600px;
    width: 100%;
}

.pay-deposit-box h1 {
    font-size: 2.5rem;
    margin-bottom: 0.5rem;
}

.pay-deposit-box > p {
    color: var(--cream);
    margin-bottom: 2rem;
}

.session-selector {
    margin: 2rem 0;
}

.session-selector h3 {
    font-size: 1.3rem;
    margin-bottom: 0.5rem;
}

.session-note {
    color: var(--cream);
    opacity: 0.7;
    font-size: 0.9rem;
    margin-bottom: 1.5rem;
}

.session-options {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    margin-bottom: 2rem;
}

.session-option {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1.25rem 1.5rem;
    background: var(--dark);
    border: 2px solid var(--light-gray);
    color: var(--white);
    cursor: pointer;
    transition: all 0.3s;
    font-family: var(--font-body);
}

.session-option:hover {
    border-color: var(--accent);
}

.session-option.selected {
    border-color: var(--accent);
    background: var(--light-gray);
}

.session-name {
    font-family: var(--font-display);
    font-size: 1.2rem;
    letter-spacing: 0.05em;
}

.session-duration {
    color: var(--accent);
    font-size: 0.9rem;
}

.deposit-amount {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1rem;
    background: var(--dark);
    margin-bottom: 1.5rem;
    font-family: var(--font-display);
    font-size: 1.2rem;
}

.deposit-amount .amount {
    color: var(--accent);
    font-size: 1.5rem;
}

.deposit-note {
    text-align: center;
    color: var(--cream);
    opacity: 0.7;
    font-size: 0.85rem;
    margin-top: 1rem;
}

.deposit-info {
    margin-top: 2rem;
    padding: 1.5rem;
    background: var(--dark);
    border-left: 4px solid var(--accent);
}

.deposit-info h3 {
    font-size: 1.1rem;
    margin-bottom: 1rem;
}

.deposit-info ul {
    list-style: none;
    color: var(--cream);
    font-size: 0.9rem;
}

.deposit-info li {
    padding: 0.4rem 0;
    padding-left: 1.5rem;
    position: relative;
}

.deposit-info li::before {
    content: '•';
    position: absolute;
    left: 0;
    color: var(--accent);
}

#pay-button:disabled {
    opacity: 0.5;
    cursor: not-allowed;
}
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
    const sessionOptions = document.querySelectorAll('.session-option');
    const payButton = document.getElementById('pay-button');
    let selectedSession = null;
    
    sessionOptions.forEach(option => {
        option.addEventListener('click', () => {
            // Remove selected from all
            sessionOptions.forEach(o => o.classList.remove('selected'));
            // Add selected to clicked
            option.classList.add('selected');
            // Store selection
            selectedSession = option.dataset.session;
            // Enable button
            payButton.disabled = false;
            payButton.textContent = 'Continue to Payment — $200';
        });
    });
    
    payButton.addEventListener('click', () => {
        if (selectedSession) {
            window.location.href = `/checkout/deposit/?session=${selectedSession}`;
        }
    });
});
</script>
