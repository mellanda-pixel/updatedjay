---
layout: base.njk
title: Payment Successful!
description: Your consultation payment was successful
permalink: /booking-success/consultation/
---

<section class="success-page">
    <div class="container">
        <div class="success-box">
            <div class="success-icon">✓</div>
            <h1>Payment Successful!</h1>
            <p>Your $50 consultation fee has been received.</p>
            
            <div class="next-step">
                <h2>Next Step: Book Your Consultation</h2>
                <p>Click below to pick a time that works for you.</p>
                <a href="https://calendly.com/jaylen-getinkedbyj/30min" class="btn btn-primary" target="_blank">Book Consultation Time</a>
            </div>

            <div class="success-info">
                <h3>What Happens Next?</h3>
                <ul>
                    <li>Pick a consultation time that works for you</li>
                    <li>You'll receive a confirmation email</li>
                    <li>We'll meet and discuss your tattoo idea</li>
                    <li>I'll give you a quote and we'll schedule your session</li>
                </ul>
            </div>

            <p class="success-note">Questions? Reach out at <a href="mailto:info@getinkedbyj.com">info@getinkedbyj.com</a></p>
        </div>
    </div>
</section>

<style>
.success-page {
    min-height: 80vh;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--dark);
    padding: 8rem 0;
}

.success-box {
    text-align: center;
    background: var(--gray);
    padding: 3rem;
    max-width: 600px;
}

.success-icon {
    width: 80px;
    height: 80px;
    background: var(--accent);
    color: var(--black);
    font-size: 3rem;
    font-weight: bold;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 1.5rem;
}

.success-box h1 {
    font-size: 2.5rem;
    margin-bottom: 0.5rem;
}

.success-box > p {
    color: var(--cream);
    font-size: 1.1rem;
    margin-bottom: 2rem;
}

.next-step {
    background: var(--dark);
    padding: 2rem;
    margin: 2rem 0;
    border: 2px solid var(--accent);
}

.next-step h2 {
    font-size: 1.5rem;
    margin-bottom: 0.5rem;
    color: var(--accent);
}

.next-step p {
    color: var(--cream);
    margin-bottom: 1.5rem;
}

.success-info {
    text-align: left;
    margin: 2rem 0;
    padding: 1.5rem;
    background: var(--dark);
}

.success-info h3 {
    font-size: 1.2rem;
    margin-bottom: 1rem;
}

.success-info ul {
    list-style: none;
    color: var(--cream);
}

.success-info li {
    padding: 0.5rem 0;
    padding-left: 1.5rem;
    position: relative;
}

.success-info li::before {
    content: '→';
    position: absolute;
    left: 0;
    color: var(--accent);
}

.success-note {
    color: var(--cream);
    opacity: 0.7;
    font-size: 0.9rem;
    margin-top: 2rem;
}

.success-note a {
    color: var(--accent);
}
</style>
